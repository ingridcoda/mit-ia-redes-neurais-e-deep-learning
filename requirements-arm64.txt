# --- DEEP LEARNING (TensorFlow otimizado para Apple Silicon) ---
tensorflow-macos==2.15.0 # TensorFlow nativo para Apple Silicon (M1/M2/M3)
tensorflow-metal==1.1.0 # Aceleração de hardware via GPU Apple Silicon

# --- DADOS E CIÊNCIA DE DADOS ---
numpy==1.26.4 # Computação numérica e manipulação de arrays
pandas==2.2.2 # Estruturas e análise de dados (DataFrames)
scikit-learn==1.4.2 # Machine learning clássico (classificação, regressão, etc)
scipy==1.13.1 # Funções matemáticas, estatísticas e científicas
matplotlib==3.8.4 # Visualização de dados (gráficos estáticos)
seaborn==0.13.2 # Visualização estatística baseada no matplotlib

# --- JUPYTER E INTERATIVIDADE ---
ipython==8.18.1
ipykernel==6.29.5
jupyter_client==8.6.2
jupyter_core==5.7.2

# --- TENSORFLOW ECOSSISTEMA ---
h5py==3.10.0 # Leitura/escrita de arquivos HDF5 (datasets grandes)
tensorboard==2.15.2 # Visualização de métricas e gráficos de treinamento
tensorboard-data-server==0.7.2
tensorflow-datasets==4.9.4 # Datasets prontos para uso com TensorFlow
tensorflow-metadata==1.14.0 # Metadados para datasets e pipelines de ML

# --- OUTROS ÚTEIS ---
tqdm==4.66.4 # Barra de progresso para loops/tarefas demoradas
joblib==1.3.2 # Paralelização e serialização de objetos Python
pillow==10.3.0 # Manipulação de imagens (abrir, salvar, converter formatos)

# --- VISUALIZAÇÃO E ESTÉTICA ---
colorama==0.4.6 # Saída colorida no terminal (logs/prints)
rich==13.7.1 # Saída colorida e formatada no terminal

# --- PARSING E UTILITÁRIOS ---
packaging==24.0
python-dateutil==2.9.0.post0
typing_extensions==4.11.0
six==1.16.0
openpyxl==3.1.2